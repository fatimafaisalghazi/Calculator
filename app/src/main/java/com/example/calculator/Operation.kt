package com.example.calculator

enum class Operation(val symbol: String) {
    Plus(symbol = " + "),
    Minus(symbol = " - "),
    Times(symbol = " × "),
    Division(symbol = " ÷ "),
    Reminder(symbol = " % ")
}